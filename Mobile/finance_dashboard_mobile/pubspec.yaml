name: finance_dashboard_mobile
description: "Personal Finance Dashboard — mobile client for juanbracho.com"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # Navigation
  go_router: ^14.6.1

  # HTTP client (Phase 2+)
  http: ^1.2.2

  # Secure API key storage (Phase 2+)
  flutter_secure_storage: ^9.2.2

  # Date & currency formatting
  intl: ^0.19.0

  # Charts
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
